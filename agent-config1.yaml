version: "1"

server:
  url: http://server:9898
  api_key: opl_3b196f4541a60429477ef6e79fc223ef5cb85863f86232a34c29e1704485a688
  insecure_skip_verify: false

agent:
  interval_seconds: 5
  self_upgrade: false
  log_collection_interval_seconds: 5

  # Services to monitor (crash detection enabled)
  monitored_services:
    - nginx
    - ssh

  logs:

    # =========================
    # Application log (single file)
    # =========================
    - name: random_app_log
      type: file
      path: /logs/random.log

    # =========================
    # System directory logs
    # =========================
    - name: system_logs
      type: directory
      path: /var/log
      include:
        - syslog
        - auth.log
        - messages
      exclude:
        - "*.gz"
        - "*.old"

    # =========================
    # Systemd journal (Linux)
    # =========================
    - name: journal_logs
      type: journal

    # =========================
    # Kernel logs
    # =========================
    - name: kernel_logs
      type: dmesg

    # =========================
    # Windows Event Logs (Windows only)
    # =========================
    # Uncomment below when running on Windows
    #
    # - name: windows_application
    #   type: windows-event
    #   channel: Application
    #
    # - name: windows_system
    #   type: windows-event
    #   channel: System
    #
    # - name: windows_security
    #   type: windows-event
    #   channel: Security

tags:
  env: preprod
  region: us-west-1
  role: go-agent
  name: agent-config1
  owner: team-a
  product: opslens-pulse
  tier: backend